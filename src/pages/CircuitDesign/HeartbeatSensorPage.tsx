import { useNavigate } from 'react-router-dom';

function HeartbeatSensorPage() {
  const navigate = useNavigate();

  return (
    <div>
        <p>Heartbeat Sensor</p>
    </div>
  );
}

export default HeartbeatSensorPage;