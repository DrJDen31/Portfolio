import { useNavigate } from 'react-router-dom';

function EmbeddedControlsMazePage() {
  const navigate = useNavigate();

  return (
    <div>
        <p>Embedded Controls Maze</p>
    </div>
  );
}

export default EmbeddedControlsMazePage;