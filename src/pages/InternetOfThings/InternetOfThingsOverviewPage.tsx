import { useNavigate } from 'react-router-dom';

function InternetOfThingsOverviewPage() {
  const navigate = useNavigate();

  return (
    <div>
        <p>IoT Overview</p>
    </div>
  );
}

export default InternetOfThingsOverviewPage;