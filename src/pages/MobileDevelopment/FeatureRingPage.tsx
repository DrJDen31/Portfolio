import { useNavigate } from 'react-router-dom';

function FeatureRingPage() {
  const navigate = useNavigate();

  return (
    <div>
        <p>FeatureRing</p>
    </div>
  );
}

export default FeatureRingPage;